warnscript_stand.discord.js [as array with muting pepole as with discord js] report with msgbox
client.on('ready', () => {
    console.log(`Logged in as ${X02UsGPctm9DdJmaIndr9wpcMKt7wfep}!`);
  });
  
  mnclient.on('message', msg => {
  const discord.js/muted.json import {  } from "module";
        msg.reply('You Have Been Muted.');
        msg.console as discord.js
        client.discord as const
        with to const discord.js botting a dm
    }
    mn.discord with replacing on screen.js
  });
  
  client.login('token');